def create_spreadsheet(title, row_count = 1000): # two variables and the second is carrying a default value for the call 
  print("Creating a spreadsheet called "+title+" with "+str(row_count)+" rows.") # calling the variables one as input and second is predefined 
  
create_spreadsheet("Downloads")
create_spreadsheet("Applications", 10)
